var React = require('react/addons')
var Form = require('../../../lib/Index')

var App = React.createClass({
      render: function () {
        return (
          <div>
            <Form />
          </div>
        )
      }
  })

module.exports = App